---
kind: ConfigMap
apiVersion: v1
metadata:
  name: drupal-config
  namespace: kubeappdrupal
data:
  # Note: This is NOT secure. Don't use this in production!
  settings.php: |-
    <?php
    $databases['default']['default'] = [
      'database' => 'kubeapp',
      'username' => 'kubeappdrupaladmin',
      'password' => 'kubeappdrupalpass',
      'prefix' => '',
      'host' => 'mariadb',
      'port' => '3306',
      'namespace' => 'Drupal\\Core\\Database\\Driver\\mysql',
      'driver' => 'mysql',
    ];
    $settings['hash_salt'] = 'OTk4MTYzYWI4N2E2MGIxNjlmYmQ2MTA4';
    $settings['trusted_host_patterns'] = ['^.+$'];
    $settings['config_sync_directory'] = 'sites/default/files/config_OTk4MTYzY';

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: drupal-files-pvc
  namespace: kubeappdrupal
spec:
  # storageClassName: local
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: drupal
  namespace: kubeappdrupal
spec:
  replicas: 1
  selector:
    matchLabels:
      app: drupal
  template:
    metadata:
      labels:
        app: drupal
    spec:
      initContainers:
        - name: init-files
          image: 'kubeappd9:latest'
          imagePullPolicy: Never
          command: ['/bin/bash', '-c']
          # args: ['cp -r /var/www/html/sites/default/files /mnt/data; chown www-data:www-data /mnt/data/ -R']
          args: ['composer install -d /var/www/html -n; chown -R www-data:www-data /var/www/html/web; chmod 755 /var/www/html/web/sites/default/files; cd /usr/sbin/ && ln -s /var/www/html/vendor/drush/drush/drush drush']
          volumeMounts:
            - mountPath: /var/www/html/sites/default/files
              name: drupal-files
      containers:
        - name: drupal
          image: 'kubeappd9:latest'
          imagePullPolicy: Never
          ports:
            - containerPort: 80
          livenessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 100
          readinessProbe:
            tcpSocket:
              port: 80
            initialDelaySeconds: 100
          volumeMounts:
            - mountPath: /var/www/html/sites/default/
              name: drupal-settings
            - mountPath: /var/www/html/sites/default/files/
              name: drupal-files
          resources:
            limits:
              cpu: '500m'
              memory: '512Mi'
            requests:
              cpu: '250m'
              memory: '256Mi'
      volumes:
        - name: drupal-settings
          configMap:
            name: drupal-config
        - name: drupal-files
          persistentVolumeClaim:
            claimName: drupal-files-pvc

---
kind: Service
apiVersion: v1
metadata:
  name: drupal
  namespace: kubeappdrupal
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: drupal
